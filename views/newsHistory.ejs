<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>News History</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/history.css">

</head>
<body>
    <nav class="navbar navbar-expand-lg fixed-top">
        <a class="navbar-brand" href="/home"> Weather App</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse " id="navbarNav">
            <ul class="navbar-nav mx-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/home">Weather</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/news">News</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/apod">NASA</a>
                </li>
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        History
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="/weather/history">Weather History</a>
                        <a class="dropdown-item" href="/news/history">News History</a>
                        <a class="dropdown-item" href="/apod/history">NASA History</a>
                    </div>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/">Log out</a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container mt-5">
        <h1 class="mb-4">News History</h1>
        <% if (historyEntries && historyEntries.length > 0) { %>
            <div class="list-group">
                <% historyEntries.forEach(entry => { %>
                    <div class="list-group-item">
                        <h5 class="mb-1"><%= entry.apiName %></h5>
                        <p class="mb-1">Request Data: country: <%= entry.requestData.country %>, category: <%= entry.requestData.category %></p>
                        <% if (entry.responseData && entry.responseData.length > 0) { %>
                            <p>Title: <%= entry.responseData[0].title %></p>
                        <% } %>
                        <small>Request Date: <%= new Date(entry.requestDate).toLocaleString() %></small>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <p>No history entries available.</p>
        <% } %>
    </div>
    <footer class="fixed-bottom bg-light text-center py-2">
        <p>Merlenkyzy Nargiz SE-2204</p>
    </footer>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>    
</body>
</html>
